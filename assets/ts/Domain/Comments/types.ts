// Domain-specific types for Comments
// Move any Comments-specific interfaces, enums, or types here as you refactor.

export interface Comment {
  id: string;
  author: string;
  content: string;
  createdAt: string;
}
