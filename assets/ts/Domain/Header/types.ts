// Domain-specific types for Header
// Move any Header-specific interfaces, enums, or types here as you refactor.

export interface HeaderConfig {
  title: string;
  subtitle?: string;
  showLogo?: boolean;
}
